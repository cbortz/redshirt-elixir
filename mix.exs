defmodule Redshirt.Mixfile do
  use Mix.Project

  def project do
    [
      app: :redshirt,
      version: "0.0.1",
      elixir: "~> 1.10",
      build_embedded: Mix.env() == :prod,
      start_permanent: Mix.env() == :prod,
      package: package(),
      description:
        "An unofficial API library for interacting with the College Football Data (CFBD) API. For information about the API, please visit https://collegefootballdata.com/. This library is automatically generated by the OpenAPI Generator project (https://github.com/OpenAPITools/openapi-generator).",
      deps: deps()
    ]
  end

  # Configuration for the OTP application
  #
  # Type "mix help compile.app" for more information
  def application do
    # Specify extra applications you'll use from Erlang/Elixir
    [extra_applications: [:logger]]
  end

  # Dependencies can be Hex packages:
  #
  #   {:my_dep, "~> 0.3.0"}
  #
  # Or git/path repositories:
  #
  #   {:my_dep, git: "https://github.com/elixir-lang/my_dep.git", tag: "0.3.0"}
  #
  # Type "mix help deps" for more examples and options
  defp deps do
    [
      {:credo, "~> 1.6", runtime: false, only: [:dev, :test]},
      {:dialyxir, "~> 1.2", runtime: false, only: [:dev, :test]},
      {:ex_doc, "~> 0.28", only: :dev, runtime: false},
      {:mix_audit, "~> 2.0", runtime: false, only: [:dev, :test]},
      {:poison, "~> 6.0"},
      {:tesla, "~> 1.4"}
    ]
  end

  defp package do
    [
      name: "redshirt",
      files: ~w(.formatter.exs config lib mix.exs README* LICENSE*)
    ]
  end
end
